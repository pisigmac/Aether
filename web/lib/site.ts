export const siteUrl = (process.env.NEXT_PUBLIC_SITE_URL || "http://127.0.0.1:13100").replace(/\/$/, "");

export const siteName = "Aether";

export const siteDescription =
  "Aether forecasts architectural weather for a repository: pressure, blast radius, extensibility, and a 24-month cost horizon. It fast-forwards the graph. It does not rewrite the code.";
